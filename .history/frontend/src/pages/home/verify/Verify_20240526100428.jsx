import "./Verify.css"

const Verify = () => {
    const [serchParams, setSearchParams] = useSearchParams ? [setSearch]
  return (
      <div>
          Verify
      </div>
  )
}

export default Verify